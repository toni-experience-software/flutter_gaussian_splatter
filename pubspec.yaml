name: flutter_gaussian_splatter
description: Flutter Gaussian Splatter
homepage: https://wearetoni.com
repository: https://github.com/toni-experience-software/flutter_gaussian_splatter
issue_tracker: https://github.com/toni-experience-software/flutter_gaussian_splatter/issues
version: 0.3.0


environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.44.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_gpu:
    sdk: flutter
  flutter_gpu_shaders: ^0.5.0
  hooks: ^2.0.0
  vector_math: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  lintervention: ^0.3.1

flutter:
  assets:
    - build/shaderbundles/flutter_gaussian_splatter.shaderbundle
